import { StyledButton } from './styled-button';

describe('StyledButton', () => {
  it('should create an instance', () => {
    const directive = new StyledButton();
    expect(directive).toBeTruthy();
  });
});
