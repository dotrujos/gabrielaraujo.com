import { SerifFont } from './serif-font';

describe('SerifFont', () => {
  it('should create an instance', () => {
    const directive = new SerifFont();
    expect(directive).toBeTruthy();
  });
});
